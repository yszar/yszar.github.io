<?xml version="1.0" encoding="utf-8"?>
<search>
  <entry>
    <title>Hexo增加Addthis分享功能</title>
    <url>/Hexo/hexo-adds-addthis-sharing-function.html</url>
    <content><![CDATA[<!--
<span id="inline-blue"> 站点配置文件 </span>
<span id="inline-purple"> 主题配置文件 </span>
<span id="inline-yellow"> 站点配置文件 </span>
<span id="inline-green"> 主题配置文件 </span>
<a id="download" href="https://git-scm.com/download/win" target="_blank" rel="noopener"><i class="fa fa-download"></i><span> Download Now</span> </a>
-->
<p><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/15777188305096.jpg" alt=""></p>
<p>我选择的是<a href="http://www.addthis.com/" target="_blank" rel="noopener">AddThis</a>作为博客的第三方分享。AdThis是国外的第三方分享,使用方便,可以用google直接登录。能自定义分享样式</p>
<h2 id="注册AddThis，选择分享按钮："><a href="#注册AddThis，选择分享按钮：" class="headerlink" title="注册AddThis，选择分享按钮："></a>注册AddThis，选择分享按钮：</h2><p><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/15777188968790.jpg" alt=""></p>
<h2 id="选择显示样式-Select-a-Tool-Type"><a href="#选择显示样式-Select-a-Tool-Type" class="headerlink" title="选择显示样式(Select a Tool Type)"></a>选择显示样式(Select a Tool Type)</h2><p>AddThis提供了7种显示样式,你可以更加左边菜单栏点击,右边会自动显示你选择的相应样式。值得注意的是。在其右上角可以切换PC和Phone显示<br><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/15777189277993.jpg" alt=""></p>
<h2 id="点击Continue配置分享按钮"><a href="#点击Continue配置分享按钮" class="headerlink" title="点击Continue配置分享按钮"></a>点击Continue配置分享按钮</h2><p>注意：不同的风格样式,配置方式不同<br><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/15777189544810.jpg" alt=""></p>
<h2 id="获取代码ID"><a href="#获取代码ID" class="headerlink" title="获取代码ID"></a>获取代码ID</h2><p>配置完成后 , 获取代码ID , 在js代码中获取<code>pubid</code>后面的ID<br><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/15777189978550.jpg" alt=""></p>
<p>编辑主题配置文件, 找到关键字<code>add_this_id</code>, 添加<code>pubid</code></p>
]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
      <tags>
        <tag>hexo</tag>
        <tag>博客</tag>
        <tag>addthis</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo博客NexT主题开启文章目录和调整样式</title>
    <url>/Hexo/hexo-blog-next-theme-opens-article-directory-and-adjusts-style.html</url>
    <content><![CDATA[<p>Hexo博客NexT主题中是有目录的，只是在默认情况下没有开启，需要我们来手动开启。</p>
<a id="more"></a>

<h1 id="修改样式文件custom-styl"><a href="#修改样式文件custom-styl" class="headerlink" title="修改样式文件custom.styl"></a>修改样式文件<code>custom.styl</code></h1><p>文章目录样式文件<code>custom.styl</code>文件位于<code>themes/next/source/css/_custom</code></p>
<p>打开后添加内容：</p>
<p>1.文章目录默认展开</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line">//文章目录默认展开</span><br><span class="line"><span class="selector-class">.post-toc</span> <span class="selector-class">.nav</span> <span class="selector-class">.nav-child</span> &#123; <span class="attribute">display</span>: block; &#125;</span><br></pre></td></tr></table></figure>
<p>2.文章目录字体大小调整</p>
<figure class="highlight css"><table><tr><td class="code"><pre><span class="line"><span class="selector-class">.post-toc</span> <span class="selector-tag">ol</span> &#123;  </span><br><span class="line">  <span class="attribute">font-size </span>: <span class="number">13px</span>;     </span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<h1 id="修改主题配置文件"><a href="#修改主题配置文件" class="headerlink" title="修改主题配置文件"></a>修改主题配置文件</h1><p>主题配置文件位于<code>themes/next/_config.yml</code></p>
<p>每行目录超长自动换行</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">toc:</span><br><span class="line">  enable: true  </span><br><span class="line">  wrap: true</span><br></pre></td></tr></table></figure>
<p>四、结果图展示<br><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/ping-mu-kuai-zhao-20191230-xia-wu73414.png" alt="屏幕快照 2019-12-30 下午7.34.14"></p>
]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>本博客服务器所有配置</title>
    <url>/Linux/all-configuration-of-this-blog-server.html</url>
    <content><![CDATA[<blockquote>
<p>之前的博客种种原因下线了，最终还是打算重新建立博客，记录一些东西，帮助自己的同时，也希望能帮助到他人。</p>
</blockquote>
<a id="more"></a>

<h2 id="服务器相关信息"><a href="#服务器相关信息" class="headerlink" title="服务器相关信息"></a>服务器相关信息</h2><ul>
<li>系统：<code>Ubuntu 18.04.3 LTS</code></li>
<li>服务商：阿里云-香港</li>
</ul>
<h2 id="安装ZSH"><a href="#安装ZSH" class="headerlink" title="安装ZSH"></a>安装ZSH</h2><h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><p>ZSH 已经被收录到了 Ubuntu 18.04 LTS 的官方软件包存储库中，安装起来也非常容易。使用如下命令就可以直接安装：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">apt update</span><br><span class="line">apt install zsh</span><br><span class="line">wget --no-check-certificate https://github.com/robbyrussell/oh-my-zsh/raw/master/tools/install.sh -O - | sh</span><br><span class="line">chsh -s /bin/zsh #将默认的shell从bash改为zsh</span><br><span class="line">reboot</span><br></pre></td></tr></table></figure>

<h3 id="为ZSH安装Powerline和Powerline字体"><a href="#为ZSH安装Powerline和Powerline字体" class="headerlink" title="为ZSH安装Powerline和Powerline字体"></a>为ZSH安装Powerline和Powerline字体</h3><p>「Powerline」是 ZSH Shell 的状态行插件，「Powerline 字体」也允许 ZSH 在 Shell 中使用不同的图标和符号。而「Powerline」和「Powerline 字体」也可以在 Ubuntu 18.04 LTS 的官方软件包存储库中找到。</p>
<p>您可以运行以下命令在 Ubuntu 18.04 LTS 上安装「Powerline」和「Powerline 字体」：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">apt install powerline fonts-powerline</span><br></pre></td></tr></table></figure>

<h3 id="安装ZSH-Powerlevel9k美化主题"><a href="#安装ZSH-Powerlevel9k美化主题" class="headerlink" title="安装ZSH Powerlevel9k美化主题"></a>安装ZSH Powerlevel9k美化主题</h3><p>Powerlevel9k 是用于 ZSH 美化的一个非常酷的主题，您可以运行以下命令在 Ubuntu 18.04 LTS 系统中安装 Powerlevel9k ZSH 主题：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">apt install zsh-theme-powerlevel9k</span><br></pre></td></tr></table></figure>
<p>装好之后执行以下命令在 Ubuntu 18.04 LTS 上启用 Powerlevel9k ZSH 主题：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">echo "source /usr/share/powerlevel9k/powerlevel9k.zsh-theme" &gt;&gt; ~/.zshrc</span><br></pre></td></tr></table></figure>
<p><img src="media/15772914852653/%E5%B1%8F%E5%B9%95%E5%BF%AB%E7%85%A7%202019-12-26%20%E4%B8%8A%E5%8D%881.08.31.png" alt="Powerlevel9k样式"></p>
<h4 id="powerlevel9k配置"><a href="#powerlevel9k配置" class="headerlink" title="powerlevel9k配置"></a>powerlevel9k配置</h4><p>默认配置参考官方说明：<br><a href="https://github.com/bhilburn/powerlevel9k/wiki/Stylizing-Your-Prompt" target="_blank" rel="noopener">https://github.com/bhilburn/powerlevel9k/wiki/Stylizing-Your-Prompt</a><br>官方推荐的各种用户配置（带各种截图）：<br><a href="https://github.com/bhilburn/powerlevel9k/wiki/Show-Off-Your-Config" target="_blank" rel="noopener">https://github.com/bhilburn/powerlevel9k/wiki/Show-Off-Your-Config</a></p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> ==== Theme Settings ====</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> PowerLevel9k</span></span><br><span class="line"><span class="meta">#</span><span class="bash"> 左侧栏目显示的要素（指定的关键字参考官网）</span></span><br><span class="line">POWERLEVEL9K_LEFT_PROMPT_ELEMENTS=(os_icon context dir vcs)</span><br><span class="line"><span class="meta">#</span><span class="bash"> 右侧栏目显示的要素</span></span><br><span class="line">POWERLEVEL9K_RIGHT_PROMPT_ELEMENTS=(status root_indicator background_jobs time virtualenv)</span><br><span class="line"><span class="meta">#</span><span class="bash">新起一行显示命令 (推荐！极其方便）</span></span><br><span class="line">POWERLEVEL9K_PROMPT_ON_NEWLINE=true</span><br><span class="line"><span class="meta">#</span><span class="bash">右侧状态栏与命令在同一行</span></span><br><span class="line">POWERLEVEL9K_RPROMPT_ON_NEWLINE=true</span><br><span class="line"><span class="meta">#</span><span class="bash">缩短目录层级</span></span><br><span class="line">POWERLEVEL9K_SHORTEN_DIR_LENGTH=1</span><br><span class="line"><span class="meta">#</span><span class="bash">缩短目录策略：隐藏上层目录中间的字</span></span><br><span class="line"><span class="meta">#</span><span class="bash">POWERLEVEL9K_SHORTEN_STRATEGY=<span class="string">"truncate_middle"</span></span></span><br><span class="line"><span class="meta">#</span><span class="bash">添加连接上下连接箭头更方便查看</span></span><br><span class="line">POWERLEVEL9K_MULTILINE_FIRST_PROMPT_PREFIX="↱"</span><br><span class="line">POWERLEVEL9K_MULTILINE_LAST_PROMPT_PREFIX="↳ "</span><br><span class="line"><span class="meta">#</span><span class="bash"> 新的命令与上面的命令隔开一行</span></span><br><span class="line"><span class="meta">#</span><span class="bash">POWERLEVEL9K_PROMPT_ADD_NEWLINE=<span class="literal">true</span></span></span><br><span class="line"><span class="meta">#</span><span class="bash"> Git仓库状态的色彩指定</span></span><br><span class="line">POWERLEVEL9K_VCS_CLEAN_FOREGROUND='blue'</span><br><span class="line">POWERLEVEL9K_VCS_CLEAN_BACKGROUND='black'</span><br><span class="line">POWERLEVEL9K_VCS_UNTRACKED_FOREGROUND='yellow'</span><br><span class="line">POWERLEVEL9K_VCS_UNTRACKED_BACKGROUND='black'</span><br><span class="line">POWERLEVEL9K_VCS_MODIFIED_FOREGROUND='red'</span><br><span class="line">POWERLEVEL9K_VCS_MODIFIED_BACKGROUND='black'</span><br></pre></td></tr></table></figure>
<h4 id="Python的Virtualenv和Pipenv虚拟环境显示问题"><a href="#Python的Virtualenv和Pipenv虚拟环境显示问题" class="headerlink" title="Python的Virtualenv和Pipenv虚拟环境显示问题"></a>Python的Virtualenv和Pipenv虚拟环境显示问题</h4><p>一般命令行里，进入虚拟环境的shell时会显示如(venv) ~$这样的。<br>但是安装这个主题后，默认是没有的。<br>你必须手动设置添加才行。</p>
<p>方法是：<br>在<code>POWERLEVEL9K_LEFT_PROMPT_ELEMENTS</code>或者<code>POWERLEVEL9K_RIGHT_PROMPT_ELEMENTS</code>中添加<code>virtualenv</code>要素，就能够显示了。<br>如：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">POWERLEVEL9K_LEFT_PROMPT_ELEMENTS=(os_icon context dir vcs virtualenv)</span><br></pre></td></tr></table></figure>
<p><img src="media/15772914852653/15772944514627.jpg" alt=""></p>
<p>显示主机型号图标问题<br>像这种显示当前主机（如Mac）图标和Git图标的问题，是需要字体支持的。</p>
<p><img src="media/15772914852653/15772946318224.jpg" alt=""></p>
<p>默认是不开启的，必须要在~/.zshrc中指定使用这种方式显示：</p>
<h4 id="字体设定-注意，字体设定必须放在主题之前）"><a href="#字体设定-注意，字体设定必须放在主题之前）" class="headerlink" title="字体设定 (注意，字体设定必须放在主题之前）"></a>字体设定 (注意，字体设定必须放在主题之前）</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">POWERLEVEL9K_MODE='nerdfont-complete'</span><br></pre></td></tr></table></figure>
<h4 id="主题设定"><a href="#主题设定" class="headerlink" title="主题设定"></a>主题设定</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">ZSH_THEME="powerlevel9k/powerlevel9k"</span><br></pre></td></tr></table></figure>
<p>上面的字体模式可选的有：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">nerdfont-complete</span><br><span class="line">awesome-fontconfig</span><br><span class="line">awesome-patched</span><br></pre></td></tr></table></figure>
<p>根据你的情况来尝试，因为不是每个都能完美无乱码显示出来。</p>
<h3 id="在ZSH-Shell上启用语法高亮显示"><a href="#在ZSH-Shell上启用语法高亮显示" class="headerlink" title="在ZSH Shell上启用语法高亮显示"></a>在ZSH Shell上启用语法高亮显示</h3><p>ZSH 有一个名为 ZSH Syntax Highlighting 的语法高亮显示插件，使用它可以突出显示 ZSH Shell 中的命令。 您可以使用如下命令进行安装：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">apt install zsh-syntax-highlighting</span><br></pre></td></tr></table></figure>
<p>安装好之后使用如下命令以启用 ZSH Syntax Highlighting 插件：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">echo "source /usr/share/zsh-syntax-highlighting/zsh-syntax-highlighting.zsh" &gt;&gt; ~/.zshrc</span><br></pre></td></tr></table></figure>

<h2 id="安装nginx"><a href="#安装nginx" class="headerlink" title="安装nginx"></a>安装nginx</h2><h3 id="安装依赖"><a href="#安装依赖" class="headerlink" title="安装依赖"></a>安装依赖</h3><p>我的 VPS 系统是 Ubuntu 18.04.3 LTS，如果你使用的是其它发行版，与包管理有关的命令请自行调整。</p>
<p>首先安装依赖库和编译要用到的工具：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">apt-get install build-essential libpcre3 libpcre3-dev zlib1g-dev unzip git</span><br></pre></td></tr></table></figure>

<h3 id="获取必要组件"><a href="#获取必要组件" class="headerlink" title="获取必要组件"></a>获取必要组件</h3><h4 id="nginx-ct"><a href="#nginx-ct" class="headerlink" title="nginx-ct"></a>nginx-ct</h4><p><code>nginx-ct</code> 模块用于启用 <code>Certificate Transparency</code> 功能。直接从 <code>github</code> 上获取源码：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">wget -O nginx-ct.zip -c https://github.com/grahamedgecombe/nginx-ct/archive/v1.3.2.zip</span><br><span class="line">unzip nginx-ct.zip</span><br></pre></td></tr></table></figure>

<h4 id="ngx-brotli"><a href="#ngx-brotli" class="headerlink" title="ngx_brotli"></a>ngx_brotli</h4><p>本站支持 Google 开发的 <code>Brotli</code> 压缩格式，它通过内置分析大量网页得出的字典，实现了更高的压缩比率，同时几乎不影响压缩 / 解压速度。</p>
<p>获取 <code>ngx_brotli</code> 源码：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">git <span class="built_in">clone</span> https://github.com/google/ngx_brotli.git</span><br><span class="line"><span class="built_in">cd</span> ngx_brotli</span><br><span class="line"></span><br><span class="line">git submodule update --init</span><br><span class="line"></span><br><span class="line"><span class="built_in">cd</span> ../</span><br></pre></td></tr></table></figure>

<h4 id="OpenSSL"><a href="#OpenSSL" class="headerlink" title="OpenSSL"></a>OpenSSL</h4><p>为了支持 <code>TLS 1.3 final</code>，需要使用 <code>OpenSSL 1.1.1</code> 正式版：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget -c  https://github.com/openssl/openssl/archive/OpenSSL_1_1_1.tar.gz</span><br><span class="line">tar xzf OpenSSL_1_1_1.tar.gz</span><br><span class="line">mv openssl-OpenSSL_1_1_1 openssl</span><br></pre></td></tr></table></figure>

<h3 id="编译并安装-Nginx"><a href="#编译并安装-Nginx" class="headerlink" title="编译并安装 Nginx"></a>编译并安装 Nginx</h3><p>接着就可以获取 <code>Nginx</code> 源码，编译并安装：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">wget -c http://nginx.org/download/nginx-1.15.2.tar.gz</span><br><span class="line">tar zxf nginx-1.15.2.tar.gz</span><br><span class="line"></span><br><span class="line"><span class="built_in">cd</span> nginx-1.15.2</span><br><span class="line"></span><br><span class="line">./configure --add-module=../ngx_brotli --with-openssl=../openssl --with-openssl-opt=<span class="string">'enable-tls1_3 enable-weak-ssl-ciphers'</span> --with-http_v2_module --with-http_ssl_module --with-http_gzip_static_module</span><br><span class="line"></span><br><span class="line">make</span><br><span class="line">make install</span><br></pre></td></tr></table></figure>

<p><code>enable-tls1_3</code> 是让 OpenSSL 支持 TLS 1.3 的关键选项；而 <code>enable-weak-ssl-ciphers</code> 的作用是让 OpenSSL 继续支持 3DES 等不安全的 Cipher Suite，如果你打算继续支持 IE8，才需要加上这个选项。</p>
<p>除了 <code>http_v2</code> 和 <code>http_ssl</code> 这两个 HTTP/2 必备模块之外，我还额外启用了 <code>http_gzip_static</code>，需要启用哪些模块需要根据自己实际情况来决定。</p>
<p>以上步骤会把 Nginx 装到 <code>/usr/local/nginx/</code> 目录，如需更改路径可以在 configure 时指定。</p>
<h3 id="WEB-站点配置"><a href="#WEB-站点配置" class="headerlink" title="WEB 站点配置"></a>WEB 站点配置</h3><p>在 Nginx 的站点配置中，以下两个参数需要修改：</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">ssl_protocols              TLSv1 TLSv1.1 TLSv1.2 TLSv1.3; # 增加 TLSv1.3</span><br><span class="line">ssl_ciphers                TLS13-AES-256-GCM-SHA384:TLS13-CHACHA20-POLY1305-SHA256:TLS13-AES-128-GCM-SHA256:TLS13-AES-128-CCM-8-SHA256:TLS13-AES-128-CCM-SHA256:EECDH+CHACHA20:EECDH+CHACHA20-draft:EECDH+ECDSA+AES128:EECDH+aRSA+AES128:RSA+AES128:EECDH+ECDSA+AES256:EECDH+aRSA+AES256:RSA+AES256:EECDH+ECDSA+3DES:EECDH+aRSA+3DES:RSA+3DES:!MD5;</span><br></pre></td></tr></table></figure>
<p>包含 <code>TLS13</code> 是 TLS 1.3 新增的 Cipher Suite，加在最前面即可；如果你不打算继续支持 IE8，可以去掉包含 <code>3DES</code> 的 Cipher Suite。</p>
<p>本博客完整的 Nginx 配置，请点击这里查看。</p>
<h3 id="验证是否支持-TLS-1-3"><a href="#验证是否支持-TLS-1-3" class="headerlink" title="验证是否支持 TLS 1.3"></a>验证是否支持 TLS 1.3</h3><p>目前最新版 Chrome 和 Firefox 都支持 TLS 1.3，但需要手动开启：</p>
<p>Chrome，将 <code>chrome://flags/</code> 中的 <code>Maximum TLS version enabled</code> 改为 <code>TLS 1.3</code>（Chrome 62 中需要将 TLS 1.3 改为 <code>Enabled (Draft)</code>；<br>Firefox，将 <code>about:config</code> 中的 <code>security.tls.version.max</code> 改为 <code>4</code>；</p>
<blockquote>
<p> <a href="https://www.ssllabs.com/ssltest/index.html" target="_blank" rel="noopener">Qualys SSL Labs’s SSL Server Test</a> 也支持验证服务端是否支持 TLS 1.3，非常方便，继续推荐。</p>
</blockquote>
<h3 id="管理脚本与自启动"><a href="#管理脚本与自启动" class="headerlink" title="管理脚本与自启动"></a>管理脚本与自启动</h3><p>为了方便管理 Nginx 服务，再创建一个管理脚本：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">vim /etc/init.d/nginx</span><br></pre></td></tr></table></figure>
<p>输入以下内容：</p>
<figure class="highlight bash"><table><tr><td class="code"><pre><span class="line"><span class="meta">#! /bin/sh</span></span><br><span class="line"></span><br><span class="line"><span class="comment">### BEGIN INIT INFO</span></span><br><span class="line"><span class="comment"># Provides:          nginx</span></span><br><span class="line"><span class="comment"># Required-Start:    $all</span></span><br><span class="line"><span class="comment"># Required-Stop:     $all</span></span><br><span class="line"><span class="comment"># Default-Start:     2 3 4 5</span></span><br><span class="line"><span class="comment"># Default-Stop:      0 1 6</span></span><br><span class="line"><span class="comment"># Short-Description: starts the nginx web server</span></span><br><span class="line"><span class="comment"># Description:       starts nginx using start-stop-daemon</span></span><br><span class="line"><span class="comment">### END INIT INFO</span></span><br><span class="line"></span><br><span class="line">PATH=/usr/<span class="built_in">local</span>/sbin:/usr/<span class="built_in">local</span>/bin:/sbin:/bin:/usr/sbin:/usr/bin</span><br><span class="line">DAEMON=/usr/<span class="built_in">local</span>/nginx/sbin/nginx</span><br><span class="line">NAME=nginx</span><br><span class="line">DESC=nginx</span><br><span class="line"></span><br><span class="line"><span class="built_in">test</span> -x <span class="variable">$DAEMON</span> || <span class="built_in">exit</span> 0</span><br><span class="line"></span><br><span class="line"><span class="comment"># Include nginx defaults if available</span></span><br><span class="line"><span class="keyword">if</span> [ -f /etc/default/nginx ] ; <span class="keyword">then</span></span><br><span class="line">  . /etc/default/nginx</span><br><span class="line"><span class="keyword">fi</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">set</span> -e</span><br><span class="line"></span><br><span class="line">. /lib/lsb/init-functions</span><br><span class="line"></span><br><span class="line"><span class="keyword">case</span> <span class="string">"<span class="variable">$1</span>"</span> <span class="keyword">in</span></span><br><span class="line">  start)</span><br><span class="line">    <span class="built_in">echo</span> -n <span class="string">"Starting <span class="variable">$DESC</span>: "</span></span><br><span class="line">    start-stop-daemon --start --quiet --pidfile /usr/<span class="built_in">local</span>/nginx/logs/<span class="variable">$NAME</span>.pid \</span><br><span class="line">        --<span class="built_in">exec</span> <span class="variable">$DAEMON</span> -- <span class="variable">$DAEMON_OPTS</span> || <span class="literal">true</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"<span class="variable">$NAME</span>."</span></span><br><span class="line">    ;;</span><br><span class="line">  stop)</span><br><span class="line">    <span class="built_in">echo</span> -n <span class="string">"Stopping <span class="variable">$DESC</span>: "</span></span><br><span class="line">    start-stop-daemon --stop --quiet --pidfile /usr/<span class="built_in">local</span>/nginx/logs/<span class="variable">$NAME</span>.pid \</span><br><span class="line">        --<span class="built_in">exec</span> <span class="variable">$DAEMON</span> || <span class="literal">true</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"<span class="variable">$NAME</span>."</span></span><br><span class="line">    ;;</span><br><span class="line">  restart|force-reload)</span><br><span class="line">    <span class="built_in">echo</span> -n <span class="string">"Restarting <span class="variable">$DESC</span>: "</span></span><br><span class="line">    start-stop-daemon --stop --quiet --pidfile \</span><br><span class="line">        /usr/<span class="built_in">local</span>/nginx/logs/<span class="variable">$NAME</span>.pid --<span class="built_in">exec</span> <span class="variable">$DAEMON</span> || <span class="literal">true</span></span><br><span class="line">    sleep 1</span><br><span class="line">    start-stop-daemon --start --quiet --pidfile \</span><br><span class="line">        /usr/<span class="built_in">local</span>/nginx/logs/<span class="variable">$NAME</span>.pid --<span class="built_in">exec</span> <span class="variable">$DAEMON</span> -- <span class="variable">$DAEMON_OPTS</span> || <span class="literal">true</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"<span class="variable">$NAME</span>."</span></span><br><span class="line">    ;;</span><br><span class="line">  reload)</span><br><span class="line">    <span class="built_in">echo</span> -n <span class="string">"Reloading <span class="variable">$DESC</span> configuration: "</span></span><br><span class="line">    start-stop-daemon --stop --signal HUP --quiet --pidfile /usr/<span class="built_in">local</span>/nginx/logs/<span class="variable">$NAME</span>.pid \</span><br><span class="line">        --<span class="built_in">exec</span> <span class="variable">$DAEMON</span> || <span class="literal">true</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"<span class="variable">$NAME</span>."</span></span><br><span class="line">    ;;</span><br><span class="line">  status)</span><br><span class="line">    status_of_proc -p /usr/<span class="built_in">local</span>/nginx/logs/<span class="variable">$NAME</span>.pid <span class="string">"<span class="variable">$DAEMON</span>"</span> nginx &amp;&amp; <span class="built_in">exit</span> 0 || <span class="built_in">exit</span> $?</span><br><span class="line">    ;;</span><br><span class="line">  *)</span><br><span class="line">    N=/etc/init.d/<span class="variable">$NAME</span></span><br><span class="line">    <span class="built_in">echo</span> <span class="string">"Usage: <span class="variable">$N</span> &#123;start|stop|restart|reload|force-reload|status&#125;"</span> &gt;&amp;2</span><br><span class="line">    <span class="built_in">exit</span> 1</span><br><span class="line">    ;;</span><br><span class="line"><span class="keyword">esac</span></span><br><span class="line"></span><br><span class="line"><span class="built_in">exit</span> 0</span><br></pre></td></tr></table></figure>
<p>增加执行权限：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">chmod a+x /etc/init.d/nginx</span><br></pre></td></tr></table></figure>

<p>现在管理 Nginx 只需使用以下命令即可：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">service nginx start|stop|restart|reload</span><br></pre></td></tr></table></figure>
<p>如果遇到以下错误</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Failed to start nginx.service: Unit nginx.service not found.</span><br></pre></td></tr></table></figure>
<p>解决方法：<br>在<code>/lib/systemd/system</code>创建<code>nginx.service</code><br>内容如下：</p>
<figure class="highlight ini"><table><tr><td class="code"><pre><span class="line"><span class="section">[Unit]</span></span><br><span class="line"><span class="attr">Description</span>=The NGINX HTTP and reverse proxy server</span><br><span class="line"><span class="attr">After</span>=syslog.target network.target remote-fs.target nss-lookup.target</span><br><span class="line"></span><br><span class="line"><span class="section">[Service]</span></span><br><span class="line"><span class="attr">Type</span>=forking</span><br><span class="line"><span class="attr">PIDFile</span>=/usr/local/nginx/logs/nginx.pid</span><br><span class="line"><span class="attr">ExecStartPre</span>=/usr/bin/nginx -t</span><br><span class="line"><span class="attr">ExecStart</span>=/usr/bin/nginx</span><br><span class="line"><span class="attr">ExecReload</span>=/bin/kill -s HUP <span class="variable">$MAINPID</span></span><br><span class="line"><span class="attr">ExecStop</span>=/bin/kill -s QUIT <span class="variable">$MAINPID</span></span><br><span class="line"><span class="attr">PrivateTmp</span>=<span class="literal">true</span></span><br><span class="line"></span><br><span class="line"><span class="section">[Install]</span></span><br><span class="line"><span class="attr">WantedBy</span>=multi-user.target</span><br></pre></td></tr></table></figure>
<p>并将nginx可执行文件拷贝至<code>/usr/bin/</code>。</p>
<p>如果要开机自动启动 Nginx，请执行以下命令：</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">update-rc.d -f nginx defaults</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Linux</category>
      </categories>
      <tags>
        <tag>linux</tag>
        <tag>服务器</tag>
      </tags>
  </entry>
  <entry>
    <title>Ubuntu编译安装Python3.8.0</title>
    <url>/Linux/Python/compile-and-install-python-380-on-ubuntu.html</url>
    <content><![CDATA[<blockquote>
<p>如果你想体验一下，请用虚拟机（感受一下就行，别当真。<br>仅测试Ubuntu</p>
</blockquote>
<a id="more"></a>


<p>##新特性<br>整体来说，有三点特别需要注意一下</p>
<p>##海象运算符</p>
<h3 id="python3-7"><a href="#python3-7" class="headerlink" title="python3.7"></a>python3.7</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line">a = <span class="string">'123'</span></span><br><span class="line"><span class="keyword">if</span> len(a):</span><br><span class="line">print(<span class="number">1</span>)</span><br></pre></td></tr></table></figure>

<h3 id="python3-8"><a href="#python3-8" class="headerlink" title="python3.8"></a>python3.8</h3><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (b := len(a)):</span><br><span class="line">print(<span class="number">1</span>)</span><br></pre></td></tr></table></figure>

<blockquote>
<p><strong>整体这样理解</strong></p>
</blockquote>
<blockquote>
<p>优先执行短赋值（借鉴golang）右边的代码<br>将右边的代码执行结果赋值给左边变量<br>再执行if判断</p>
</blockquote>
<p>展示张图片<br><img src="https://img-blog.csdnimg.cn/20190517190032161.png?x-oss-process=image/watermark,type_ZmFuZ3poZW5naGVpdGk,shadow_10,text_aHR0cHM6Ly9ibG9nLmNzZG4ubmV0L3FxXzQyMzQ2NDE0,size_16,color_FFFFFF,t_70" alt=""></p>
<h3 id="编译字节码可以单独指定存放位置"><a href="#编译字节码可以单独指定存放位置" class="headerlink" title="编译字节码可以单独指定存放位置"></a>编译字节码可以单独指定存放位置</h3><p>说白了可以通过设置环境变量 <code>PYTHONPYCACHEPREFIX</code>来指定这个生成路径，你也可以在代码中使用 <code>sys.pycache_prefix</code>来获取这个生成路径</p>
<p>最直接的作用就是分发Python代码的时候可以只分发编译字节码包，从而一定程度上提高了Python的安全性（可能）</p>
<p>在Python3.8之前，<code>finally</code> 中加 <code>continue</code> 语句是不合法的。<br><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/25/15772765508044.jpg" alt=""></p>
<p>Python3.8之后这个限制被移除了。<br><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/25/15772765993531.jpg" alt=""></p>
<p>执行结果如下<br><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/25/15772766129872.jpg" alt=""></p>
<p>更多细节，<a href="https://docs.python.org/3.8/whatsnew/3.8.html" target="_blank" rel="noopener">Python3.8.0a4官方文档</a></p>
<p>在ubuntu1804之后python3.7存在，暂不替换（不设置软连接）。<br>python3.8下载（支持Linux，Windows，Mac）</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">wget https://www.python.org/ftp/python/3.8.0/Python-3.8.0a4.tar.xz</span><br></pre></td></tr></table></figure>

<h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><h4 id="先决条件（这样可以避免两个个报错）"><a href="#先决条件（这样可以避免两个个报错）" class="headerlink" title="先决条件（这样可以避免两个个报错）"></a>先决条件（这样可以避免两个个报错）</h4><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">apt-get install zlib* -y</span><br><span class="line">apt-get install libffi-dev -y</span><br><span class="line">unxz Python-3.8.0a4.tar.xz</span><br><span class="line">tar -zxvf Python-3.8.0a4</span><br><span class="line"></span><br><span class="line"><span class="meta">#</span><span class="bash"> 解压Python-3.8.0a4.tar.xz包时，这里推荐手动提取，这样可以省去两条命令。如果python是在一个文件夹下，请确认非root用户拥有该文件夹的全县（递归）</span></span><br><span class="line">chown -R [非root]:[非root] Python-3.8.0a4</span><br><span class="line"></span><br><span class="line">./configure --prefix=/usr/local/python3 --with-ssl</span><br></pre></td></tr></table></figure>

<p>如果是Centos系统，安装报错均可在网上查阅到</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">zipimport.ZipImportError: can't decompress data; zlib not available</span><br><span class="line">...</span><br><span class="line">make: *** [Makefile:1168：install] 错误 </span><br><span class="line"><span class="meta">#</span><span class="bash">权限问题，需要非root用户拥有解压好的python文件夹权限。</span></span><br></pre></td></tr></table></figure>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">apt-get install libffi-dev -y</span><br><span class="line"><span class="meta">#</span><span class="bash"> 这条命令在解决 ModuleNotFoundError: No module named <span class="string">'_ctypes'</span>无模块问题</span></span><br></pre></td></tr></table></figure>
<p>出现以下，便是安装完成了！</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">Collecting setuptools</span><br><span class="line">Collecting pip</span><br><span class="line">Installing collected packages: setuptools, pip</span><br><span class="line">Successfully installed pip-19.0.3 setuptools-40.8.0</span><br></pre></td></tr></table></figure>

<p>最后，可以自己编写代码来感受一下<code>python3.8</code>的一些特别之处！</p>
]]></content>
      <categories>
        <category>Linux</category>
        <category>Python</category>
      </categories>
      <tags>
        <tag>python</tag>
        <tag>linux</tag>
      </tags>
  </entry>
  <entry>
    <title>利用gulp对Hexo博客压缩并一键之部署</title>
    <url>/Hexo/use-gulp-to-compress-and-deploy-hexo-blog-with-one-click.html</url>
    <content><![CDATA[<h1 id="说在前面"><a href="#说在前面" class="headerlink" title="说在前面"></a>说在前面</h1><p>压缩页面无非就是减小页面大小加快博客访问时间</p>
<a id="more"></a>

<h1 id="实现"><a href="#实现" class="headerlink" title="实现"></a>实现</h1><h2 id="安装依赖"><a href="#安装依赖" class="headerlink" title="安装依赖"></a>安装依赖</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm i gulp gulp-htmlclean gulp-htmlmin gulp-minify-css gulp-uglify -s</span><br></pre></td></tr></table></figure>

<h2 id="gulp配置"><a href="#gulp配置" class="headerlink" title="gulp配置"></a>gulp配置</h2><p>在根目录下创建<code>gulpfile.js</code>文件</p>
<p><code>gulpfile.js</code></p>
<figure class="highlight js"><table><tr><td class="code"><pre><span class="line"><span class="keyword">var</span> gulp = <span class="built_in">require</span>(<span class="string">'gulp'</span>);</span><br><span class="line"><span class="keyword">var</span> minifycss = <span class="built_in">require</span>(<span class="string">'gulp-minify-css'</span>);</span><br><span class="line"><span class="keyword">var</span> uglify = <span class="built_in">require</span>(<span class="string">'gulp-uglify'</span>);</span><br><span class="line"><span class="keyword">var</span> htmlmin = <span class="built_in">require</span>(<span class="string">'gulp-htmlmin'</span>);</span><br><span class="line"><span class="keyword">var</span> htmlclean = <span class="built_in">require</span>(<span class="string">'gulp-htmlclean'</span>);</span><br><span class="line"><span class="comment">// 压缩 public 目录 css</span></span><br><span class="line">gulp.task(<span class="string">'minify-css'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> gulp.src(<span class="string">'./public/**/*.css'</span>)</span><br><span class="line">        .pipe(minifycss())</span><br><span class="line">        .pipe(gulp.dest(<span class="string">'./public'</span>));</span><br><span class="line">&#125;);</span><br><span class="line"><span class="comment">// 压缩 public 目录 html</span></span><br><span class="line">gulp.task(<span class="string">'minify-html'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> gulp.src(<span class="string">'./public/**/*.html'</span>)</span><br><span class="line">        .pipe(htmlclean())</span><br><span class="line">        .pipe(htmlmin(&#123;</span><br><span class="line">            removeComments: <span class="literal">true</span>,</span><br><span class="line">            minifyJS: <span class="literal">true</span>,</span><br><span class="line">            minifyCSS: <span class="literal">true</span>,</span><br><span class="line">            minifyURLs: <span class="literal">true</span>,</span><br><span class="line">        &#125;))</span><br><span class="line">        .pipe(gulp.dest(<span class="string">'./public'</span>))</span><br><span class="line">&#125;);</span><br><span class="line"><span class="comment">// 压缩 public/js 目录 js</span></span><br><span class="line">gulp.task(<span class="string">'minify-js'</span>, <span class="function"><span class="keyword">function</span> (<span class="params"></span>) </span>&#123;</span><br><span class="line">    <span class="keyword">return</span> gulp.src(<span class="string">'./public/**/*.js'</span>)</span><br><span class="line">        .pipe(uglify())</span><br><span class="line">        .pipe(gulp.dest(<span class="string">'./public'</span>));</span><br><span class="line">&#125;);</span><br><span class="line"><span class="comment">// 执行 gulp 命令时执行的任务</span></span><br><span class="line">gulp.task(<span class="string">'default'</span>, [</span><br><span class="line">    <span class="string">'minify-html'</span>, <span class="string">'minify-css'</span>, <span class="string">'minify-js'</span></span><br><span class="line">]);</span><br></pre></td></tr></table></figure>

<h1 id="一键部署"><a href="#一键部署" class="headerlink" title="一键部署"></a>一键部署</h1><p>在<code>package.json</code>中加入如下script</p>
<figure class="highlight"><table><tr><td class="code"><pre><span class="line">"scripts": &#123;</span><br><span class="line">  "push": "hexo cl &amp;&amp; hexo g &amp;&amp; gulp &amp;&amp; hexo d"</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>
<p>然后在部署的时候只需要运行</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">npm run push</span><br></pre></td></tr></table></figure>]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>通过acme.sh申请Let&#39;s</title>
    <url>/%E5%BB%BA%E7%AB%99%E7%9B%B8%E5%85%B3/apply-for-lets-via-acmesh.html</url>
    <content><![CDATA[<p>Let’s Encrypt 已经可以申请通配符证书了，详细通过 acme.sh 申请 Let’s Encrypt 证书的请移步《Linux 下使用 acme.sh 申请和管理 Let’s Encrypt 证书》，我是已经有了 Let’s Encrypt 证书的情况下删除证书重新申请通配符证书的，不过方法都大同小异。在这次申请 Let’s Encrypt 通配符证书的时候也遇到一些问题，现在将申请的过程及问题解决办法记录下来分享给大家，以便大家参考。</p>
<a id="more"></a>

<h1 id="申请-Let’s-Encrypt通配符证书步骤"><a href="#申请-Let’s-Encrypt通配符证书步骤" class="headerlink" title="申请 Let’s Encrypt通配符证书步骤"></a>申请 Let’s Encrypt通配符证书步骤</h1><h2 id="升级-acme-sh"><a href="#升级-acme-sh" class="headerlink" title="升级 acme.sh"></a>升级 acme.sh</h2><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">acme.sh –upgrade</span><br></pre></td></tr></table></figure>
<p>PS：我升级总是失败，但是升级失败的情况好像也能申请到通配符证书，如果到第 5 步失败的话，就是升级 acme.sh 失败导致，实在不行就放弃通配符证书，改回原来的域名证书。所以想要申请通配符证书需要慎重。</p>
<p>如何通过<code>acme.sh</code>申请Let’s Encrypt通配符证书？ - 第2张 - boke112联盟(boke112.com)</p>
<p>2、查看证书列表</p>
<p>acme.sh –list<br>3、删除证书</p>
<p>acme.sh remove Main_Domain(证书的主域名，上述证书列表中可看见)<br>4、获取 Aliyun 域名 API 的密钥，在阿里云官网登录的情况下点击<a href="https://ak-console.aliyun.com/#/accesskey即可看到自己的密钥，没有就直接创建一个即可。非阿里云的请移步《如何使用" target="_blank" rel="noopener">https://ak-console.aliyun.com/#/accesskey即可看到自己的密钥，没有就直接创建一个即可。非阿里云的请移步《如何使用</a> DNS API》查阅相对应的 API 的办法。</p>
<p>5、获得密钥之后分别输入以下命令</p>
<p>export Ali_Key=”sdfsdfsdfljlbjkljlkjsdfoiwje”<br>export Ali_Secret=”jlsdflanljkljlfdsaklkjflsa”<br>acme.sh –issue –dns dns_ali -d yigujin.cn -d *.yigujin.cn –force<br>记得修改以上的域名为自己的域名哦<br>6、获取通配符证书之后，把证书复制到另一个地方待用</p>
<p>acme.sh –installcert -d yigujin.cn -d *.yigujin.cn <br>–key-file  /etc/nginx/ssl/yigujin.cn.key  <br>–fullchain-file /etc/nginx/ssl/yigujin.cn.cer <br>–reloadcmd  “service nginx force-reload”<br>记得修改以上的域名为自己的域名，存放证书的路径也要自行修改哦。<br>申请 Let’s Encrypt通配符证书遇到一个问题<br>不知道是不是因为升级 acme.sh 失败的缘故还是什么问题，我申请得到的通配符证书复制到指定位置存放之后，.cer 文件只有域名的证书内容，而无 ca 证书的内容，所以就会出现两个问题：</p>
<p>1、在服务器显示：nginx：[warn] “ssl_stapling” ignored,issuer certificate not found 的警告信息。</p>
<p>如何通过acme.sh申请Let’s Encrypt通配符证书？ - 第3张 - boke112联盟(boke112.com)</p>
<p>2、在<a href="https://myssl.com/检测得到的等级从" target="_blank" rel="noopener">https://myssl.com/检测得到的等级从</a> A+变成了 B。</p>
<p>其实以上两个问题都是同一个原因导致的，就是我们通过以下命令：</p>
<p>–fullchain-file /etc/nginx/ssl/yigujin.cn.cer <br>所得到的 yigujin.cn.cer 证书里面根本就没有 CA 的证书的，而同样的方法申请非通配符的证书，cer 文件里面都含有 CA 证书，这个目前还不知道是什么原因导致。</p>
<p>既然知道了原因，那么解决办法就有两个，一个是放弃通配符证书，另一个就是人工添加 CA 证书。这里重点介绍人工添加 CA 证书的办法。</p>
<p>1、复制 CA 证书到位置</p>
<p>acme.sh –installcert -d yigujin.cn -d *.yigujin.cn <br>–key-file  /etc/nginx/ssl /yigujin.cn.key  <br>–fullchain-file /etc/nginx/ssl /yigujin.cn.cer <br>–ca-file /etc/nginx/ssl/yigujin.cn.ca.cer <br>–reloadcmd  “service nginx force-reload”<br>2、在服务器配置文件中找到以下代码：</p>
<p>ssl_certificate /etc/nginx/ssl/yigujin.cn.cer;<br>ssl_certificate_key  /etc/nginx/ssl/yigujin.cn.key;<br>在下方添加 CA 证书</p>
<p>ssl_trusted_certificate /etc/nginx/ssl/yigujin.cn.ca.cer;<br>不想在配置文件中添加一天 CA 证书命令的户，好像是可以直接把 CA 证书的内容添加到 yigujin.cn.cer 证书的后面保存也能解决问题。</p>
<p>3、平滑重启 nginx 后，警告信息 nginx：[warn] “ssl_stapling” ignored,issuer certificate not found 已经消失不见了。</p>
<p>4、将 yigujin.cn.cer 和 yigujin.cn.ca.cer 的内容合二为一，其实就是以前的 cer 证书，我们需要将这些内容添加到相应的 CDN 证书中，要不然会说证书链不完整。360 网站卫士的就是把这两个内容合成一个 CRT 文件上传。</p>
<p>总结<br>我申请 Let’s Encrypt 通配符证书所遇到的问题不知道是个案还是什么，不过本文所列举的问题及解决办法都是真实发生的，目前我的懿古今和 boke112 导航已经使用上了 Let’s Encrypt 通配符证书，运行一切良好。如果你在申请 Let’s Encrypt 通配符证书也遇到相类似的问题，你可以试着按照我的办法去解决看看。申请 Let’s Encrypt 证书（非通配符证书）不会遇到证书链不完整的情况，也不需要人工合成证书。</p>
]]></content>
      <categories>
        <category>建站相关</category>
      </categories>
      <tags>
        <tag>linux</tag>
        <tag>服务器</tag>
        <tag>ssl</tag>
        <tag>nginx</tag>
      </tags>
  </entry>
  <entry>
    <title>Hexo，添加标题翻译插件</title>
    <url>/Hexo/hexo-add-title-translation-plugin.html</url>
    <content><![CDATA[<h1 id="文章背景"><a href="#文章背景" class="headerlink" title="文章背景"></a>文章背景</h1><p>  hexo生成的默认文章链接格式是这样的：<a href="https://blog.mariojd.cn/2013/07/14/" target="_blank" rel="noopener">https://blog.mariojd.cn/2013/07/14/</a><Markdown file name>/，这个配置在hexo根目录下的_config.yml里面：permalink: :year/:month/:day/:title/，这种默认的配置缺点很明显，当文件名是中文的时候url链接里就有中文出现，看起来low的同时也非常不利于seo优化，下面介绍两种解决方案。</p>
<a id="more"></a>

<h2 id="方案一：添加urlname属性-手动"><a href="#方案一：添加urlname属性-手动" class="headerlink" title="方案一：添加urlname属性(手动)"></a>方案一：添加urlname属性(手动)</h2><p>  在md文件的<code>Front-matter</code>区域新增<code>urlname</code>属性，可以是文章的英文Title也可以是其它自定义标识，所以每次编写Markdown你都得这么做，参考如下：
  </p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">---</span><br><span class="line">title: 解决百度蜘蛛无法爬取Hexo博客的问题</span><br><span class="line">urlname: solve-the-problem-that-baidu-spider-cannot-crawl-hexo-blog</span><br><span class="line">date: 2018-6-28 </span><br><span class="line">categories: hexo</span><br><span class="line">author: jiuyang</span><br><span class="line">tags: hexo</span><br><span class="line">cover_picture: http:&#x2F;&#x2F;xxx.xx&#x2F;xxx.jpg</span><br><span class="line">top: 1</span><br><span class="line">---</span><br></pre></td></tr></table></figure>
<h2 id="方案二：利用hexo插件-自动"><a href="#方案二：利用hexo插件-自动" class="headerlink" title="方案二：利用hexo插件(自动)"></a>方案二：利用hexo插件(自动)</h2><p>  在hexo plugins搜索<code>link</code>，已经有几个插件可以将url转换成不同的格式(如下)，我这里选用了<code>hexo-translate-title</code>，也是觉得这个比较适合自身风格。</p>
<figure class="highlight plain"><table><tr><td class="code"><pre><span class="line">hexo-translate-title: Translate the chinese title of Hexo blog to english words automatially</span><br><span class="line">hexo-permalink-pinyin: A Hexo plugin which convert Chinese title to transliterate permalink.</span><br><span class="line">hexo-abbrlink: Auto create one and only link for every post for hexo</span><br><span class="line">hexo-number-title: The hexo blog post url is displayed as a number.</span><br></pre></td></tr></table></figure>

<p>  下面简单介绍一下安装操作过程。</p>
<h3 id="安装"><a href="#安装" class="headerlink" title="安装"></a>安装</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">// 用cnpm会比较好</span><br><span class="line">npm install hexo-translate-title --save</span><br></pre></td></tr></table></figure>

<h3 id="配置"><a href="#配置" class="headerlink" title="配置"></a>配置</h3><p>  修改hexo根项目下的<code>_config.yml</code>，建议用google翻译毕竟贴地气，但实测并不稳定，所以也可以用<code>baidu_no_appid</code>，这样也不需要额外的认证配置。</p>
<figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">translate_title:</span></span><br><span class="line">  <span class="attr">translate_way:</span> <span class="string">google</span>  <span class="comment"># google,youdao,baidu_with_appid,baidu_no_appid</span></span><br><span class="line">  <span class="attr">is_need_proxy:</span> <span class="literal">false</span>     <span class="comment"># true | false</span></span><br><span class="line">  <span class="comment"># proxy_url: http://localhost:50018 # Your proxy_url</span></span><br><span class="line">  <span class="comment"># youdao_api_key: '' # Your youdao_api_key</span></span><br><span class="line">  <span class="comment"># youdao_keyfrom: xxxx-blog # Your youdao_keyfrom</span></span><br><span class="line">  <span class="comment"># baidu_appid: '' # Your baidu_appid</span></span><br><span class="line">  <span class="comment"># baidu_appkey: '' # Your baidu_appkey</span></span><br></pre></td></tr></table></figure>
<h3 id="修改原链接格式："><a href="#修改原链接格式：" class="headerlink" title="修改原链接格式："></a>修改原链接格式：</h3><figure class="highlight yml"><table><tr><td class="code"><pre><span class="line"><span class="attr">permalink:</span> <span class="string">:year/:month/:day/:title/</span></span><br><span class="line"><span class="attr">permalink:</span> <span class="string">:year:month:day/:translate_title.html</span></span><br></pre></td></tr></table></figure>
<h3 id="测试"><a href="#测试" class="headerlink" title="测试"></a>测试</h3><p>  用<code>hexo d</code>重新生成，留意控制台输出以及public目录下是否有生成相应的<code>&lt;hexo-translate-title&gt;.html</code>文件，如果有代表成功了，再用<code>hexo d</code>部署即可。</p>
<h3 id="记坑"><a href="#记坑" class="headerlink" title="记坑"></a>记坑</h3><p>  按上述流程安装、配置和部署，但生成的链接都是<code>undefined.html</code>，随后给作者提issue，回复后留意<code>hexo g</code>输出，并且看了源码，用<code>hexo config</code>查看配置信息后未发现问题，最终定位为Google翻译服务的不稳定。</p>
]]></content>
      <categories>
        <category>Hexo</category>
      </categories>
      <tags>
        <tag>hexo</tag>
      </tags>
  </entry>
  <entry>
    <title>Pycharm 远端同步设置（SSH Interpreter）</title>
    <url>/Python/pycharm-remote-synchronization-settings-ssh-interpreter.html</url>
    <content><![CDATA[<h2 id="新建项目"><a href="#新建项目" class="headerlink" title="新建项目"></a>新建项目</h2><p><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/sshinterpreter1.png" alt="SSHInterpreter1"></p>
<a id="more"></a>

<h2 id="设置"><a href="#设置" class="headerlink" title="设置"></a>设置</h2><p>设置本地项目路径1，设置SSH编译器2，设置远端项目路径3（会自动与本地映射）<br><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/sshinterpreter2.png" alt="SSHInterpreter2"><br>interpreter 点击 “….”，选择<code>SSH interpreter</code><br><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/ssh4interpreter3.png" alt="SSHInterpreter3"><br><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/sshinterpreter4.png" alt="SSHInterpreter4"><br>最后设置一下 自动同步。<br><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/sshinterpreter5.png" alt="SSHInterpreter5"></p>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>python</tag>
        <tag>pycharm</tag>
      </tags>
  </entry>
  <entry>
    <title>用Django实现Github Webhook</title>
    <url>/Python/github-webhook-with-django.html</url>
    <content><![CDATA[<blockquote>
<p>Webhooks 用中文翻译即网络钩子，以 GitHub 为例，当你向 Github 向发送 push 或 pull 等请求事件时，会触发指定的 URL，一般是向 URL 发送 POST 请求，我们收到 POST 请求就可以根据不同的事件进行相应的处理。</p>
</blockquote>
<a id="more"></a>

<p>下面我们的例子演示了利用 <code>Github Webhooks</code> 将 <code>Hexo</code> 部署到 VPS。</p>
<h1 id="创建-Webhook"><a href="#创建-Webhook" class="headerlink" title="创建 Webhook"></a>创建 Webhook</h1><p>创建 webhook 项目</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> django-admin startproject webhook</span></span><br></pre></td></tr></table></figure>
<p>创建 core app</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> django-admin startapp core</span></span><br></pre></td></tr></table></figure>
<p>项目 urls.py</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> django.contrib <span class="keyword">import</span> admin</span><br><span class="line"><span class="keyword">from</span> django.urls <span class="keyword">import</span> path, include</span><br><span class="line"></span><br><span class="line">urlpatterns = [</span><br><span class="line">    path(<span class="string">'admin/'</span>, admin.site.urls),</span><br><span class="line">    path(<span class="string">''</span>, include(<span class="string">'core.urls'</span>)),</span><br><span class="line">]</span><br></pre></td></tr></table></figure>

<p>core/urls.py</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> django.urls <span class="keyword">import</span> path</span><br><span class="line"><span class="keyword">from</span> . <span class="keyword">import</span> views</span><br><span class="line"></span><br><span class="line">urlpatterns = [</span><br><span class="line">    path(<span class="string">'webhook'</span>, views.webhook, name=<span class="string">'webhook'</span>),</span><br><span class="line">]</span><br></pre></td></tr></table></figure>
<p>在 Github 项目右侧的 <code>Settings</code> 中找到 <code>Webhooks</code>，填入你上面定义的外网链接，Secret 暂时留空，其他设置见下图，然后点击 Add Webhook。</p>
<p><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/15776407068706.jpg" alt=""></p>
<p><code>core/views.py</code> 先写一个简单的小例子。</p>
<p><code>csrf_exempt</code> 装饰器表示不需要 csrf 认证，我们这里使用的密钥认证，稍后再说。</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> django.http <span class="keyword">import</span> HttpResponse</span><br><span class="line"><span class="keyword">from</span> django.views.decorators.csrf <span class="keyword">import</span> csrf_exempt</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@csrf_exempt</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">webhook</span><span class="params">(request)</span>:</span></span><br><span class="line">    <span class="keyword">return</span> HttpResponse(<span class="string">'pong'</span>)</span><br></pre></td></tr></table></figure>
<p>将服务端在 VPS 上运行起来，这里推荐使用 Pycharm 的 SSH Interpreter 的，方便远程调试。<br><a href="http://localhost:4000/Python/pycharm-remote-synchronization-settings-ssh-interpreter.html" target="_blank" rel="noopener">SSH Interpreter设置传送门</a><br>注意：需要将运行 IP 改成 <code>0.0.0.0</code>，同时要向 <code>settings.py</code> 中的 <code>ALLOWED_HOSTS</code> 列表添加你的 VPS 主机名。</p>
<p>我们在 Github Webhook 上主动发送请求测试下，查看响应，得到的是预期的 <code>pong</code>。</p>
<p><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/15776426446826.jpg" alt=""></p>
<h1 id="保证-Webhook的安全"><a href="#保证-Webhook的安全" class="headerlink" title="保证 Webhook的安全"></a>保证 Webhook的安全</h1><h2 id="IP-白名单"><a href="#IP-白名单" class="headerlink" title="IP 白名单"></a>IP 白名单</h2><p>此刻任何人在浏览器或者其他地方都可以直接访问该链接</p>
<p><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/15776426837621.jpg" alt=""></p>
<p>然而我们只想接收来自 Github 的请求，其他的理都不想理，因此我们可以建立 Github 白名单，只允许白名单的 IP 访问。幸而 Github 已经提供了所有最新的服务器 IP。通过 <a href="https://api.github.com/meta" target="_blank" rel="noopener">https://api.github.com/meta</a> 就可以查看到。</p>
<p>要获取用户的 IP，可以从请求头下手。</p>
<ul>
<li><code>REMOTE_ADDR</code> 该请求头记录了 TCP 连接地址，但是如果用户使用了代理，就变成了代理 IP。所以一般先看 <code>HTTP_X_FORWARDED_FOR</code>。</li>
<li><code>HTTP_X_FORWARDED_FOR</code> 该请求头记录了依次转发的每个服务的 IP，一般情况下，第一个就是客户端的真实地址。但是这个属性可以篡改，所以也不一定可信。<br>因此单纯通过 HTTP 请求来获取真实的 IP，是不现实的，不过大多数情况下是准确的，所以我们不过多考虑其他复杂的因素。</li>
</ul>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_client_ip</span><span class="params">(request)</span>:</span></span><br><span class="line">    x_forwarded_for = request.META.get(<span class="string">'HTTP_X_FORWARDED_FOR'</span>)</span><br><span class="line">    <span class="keyword">if</span> x_forwarded_for:</span><br><span class="line">        ip = x_forwarded_for.split(<span class="string">','</span>)[<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        ip = request.META.get(<span class="string">'REMOTE_ADDR'</span>)</span><br><span class="line">    <span class="keyword">return</span> ip</span><br></pre></td></tr></table></figure>
<p>但是 Github 提供的 meta API 返回的是一个 IP 网段，我们还需要借助 <code>ipaddress</code> 模块进行判断。</p>
<p>判断一个 IP 在不在某个网段中，如下：</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">In [<span class="number">1</span>]: <span class="keyword">from</span> ipaddress <span class="keyword">import</span> ip_address, ip_network</span><br><span class="line"></span><br><span class="line">In [<span class="number">2</span>]: ip_address(<span class="string">'185.199.108.233'</span>) <span class="keyword">in</span> ip_network(<span class="string">'185.199.108.0/22'</span>)</span><br><span class="line">Out[<span class="number">2</span>]: <span class="literal">True</span></span><br><span class="line"></span><br><span class="line">In [<span class="number">3</span>]: ip_address(<span class="string">'185.199.129.15'</span>) <span class="keyword">in</span> ip_network(<span class="string">'185.199.108.0/22'</span>)</span><br><span class="line">Out[<span class="number">3</span>]: <span class="literal">False</span></span><br></pre></td></tr></table></figure>

<p><code>core/views.py</code> 继续改进如下</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> django.http <span class="keyword">import</span> HttpResponse, HttpResponseForbidden</span><br><span class="line"><span class="keyword">from</span> django.views.decorators.csrf <span class="keyword">import</span> csrf_exempt</span><br><span class="line"><span class="keyword">import</span> requests</span><br><span class="line"><span class="keyword">from</span> ipaddress <span class="keyword">import</span> ip_address, ip_network</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_client_ip</span><span class="params">(request)</span>:</span></span><br><span class="line">    x_forwarded_for = request.META.get(<span class="string">'HTTP_X_FORWARDED_FOR'</span>)</span><br><span class="line">    <span class="keyword">if</span> x_forwarded_for:</span><br><span class="line">        ip = x_forwarded_for.split(<span class="string">','</span>)[<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        ip = request.META.get(<span class="string">'REMOTE_ADDR'</span>)</span><br><span class="line">    <span class="keyword">return</span> ip</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@csrf_exempt</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">webhook</span><span class="params">(request)</span>:</span></span><br><span class="line">    client_ip = get_client_ip(request)</span><br><span class="line">    print(client_ip)</span><br><span class="line">    whitelist = requests.get(<span class="string">'https://api.github.com/meta'</span>).json()[<span class="string">'hooks'</span>]</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> network <span class="keyword">in</span> whitelist:</span><br><span class="line">        <span class="keyword">if</span> ip_address(client_ip) <span class="keyword">in</span> ip_network(network):</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> HttpResponseForbidden(<span class="string">'你没有权限访问！'</span>)</span><br><span class="line">    <span class="keyword">return</span> HttpResponse(<span class="string">'pong'</span>)</span><br></pre></td></tr></table></figure>


<p>用 Github 测试成功</p>
<p><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/15776428863934.jpg" alt=""></p>
<p>用浏览器测试，已经被阻止了</p>
<p><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/15776429000755.jpg" alt=""></p>
<p>但是速度有点慢，因为每次访问都有从 API 去获取白名单，因此我将其放到 <code>settings.py</code> 中，只在每次启动时获取一次。</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">IP_WHITELIST = requests.get(<span class="string">'https://api.github.com/meta'</span>).json()[<span class="string">'hooks'</span>]</span><br></pre></td></tr></table></figure>

<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> django.conf <span class="keyword">import</span> settings</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="meta">@csrf_exempt</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">webhook</span><span class="params">(request)</span>:</span></span><br><span class="line">    client_ip = get_client_ip(request)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> network <span class="keyword">in</span> settings.IP_WHITELIST:</span><br><span class="line">        <span class="keyword">if</span> ip_address(client_ip) <span class="keyword">in</span> ip_network(network):</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> HttpResponseForbidden(<span class="string">'你没有权限访问！'</span>)</span><br><span class="line">    <span class="keyword">return</span> HttpResponse(<span class="string">'pong'</span>)</span><br></pre></td></tr></table></figure>
<h2 id="密钥-Token"><a href="#密钥-Token" class="headerlink" title="密钥 Token"></a>密钥 Token</h2><p>仅仅是限制 IP 还不够安全，我们还要采用 token 校验。</p>
<p>我们先生成一个随机字符串，Django 内置的加密模块就有。</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">In [<span class="number">4</span>]: <span class="keyword">from</span> django.utils.crypto <span class="keyword">import</span> get_random_string</span><br><span class="line"></span><br><span class="line">In [<span class="number">5</span>]: get_random_string(<span class="number">50</span>)</span><br><span class="line">Out[<span class="number">5</span>]: <span class="string">'9IPDq6yjYcBe7jyvgZfzQtAHfU3jF9DAixsEiACM4OlsrzIYzM'</span></span><br></pre></td></tr></table></figure>

<p>填写到 <code>settings.py</code>，注意不要将其泄露，因此我们的所有加密都基于此密钥。如果要上传到一些公开的仓库中，建议用 Django-environ 将其存到 <code>.env</code> 中并加入到 <code>.gitignore</code> 中。</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line">GITHUB_WEBHOOK_KEY = <span class="string">'9IPDq6yjYcBe7jyvgZfzQtAHfU3jF9DAixsEiACM4OlsrzIYzM'</span></span><br></pre></td></tr></table></figure>

<p>将该密钥同时填写到 <code>Github Webhooks</code> 中，点击 Update webhook。</p>
<p><img src="http://mweb-tangjiuyang-com.oss-cn-beijing.aliyuncs.com/2019/12/30/15776431184422.jpg" alt=""></p>
<p>Github Webhook 将每次请求称为 Delivery，Delivery 的请求头格式如下：</p>
<ul>
<li>X-GitHub-Event 事件类型，例如 ping</li>
<li>X-GitHub-Delivery 此次 Delivery 的 GUID 唯一标识，例如 52d21780-4e19-11e9-9085-ded6652d231c</li>
<li>X-Hub-Signature 签名，例如 sha1=c1c05b438b13a46ba7a9c914a3e1852a4b8713a6<br>这里就要用到 <code>X-Hub-Signature</code> 签名，其实现原理很简单，采用的是对称加密，用的 HMAC 加密方式，哈希算法是 <code>sha1</code>，key 是上面填写的 <code>secret</code>，加密内容为 body 部分，得到的加密字符串是十六进制的。</li>
</ul>
<p><code>core/views.py</code> 继续改进如下：</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">from</span> django.http <span class="keyword">import</span> HttpResponse, HttpResponseForbidden</span><br><span class="line"><span class="keyword">from</span> django.views.decorators.csrf <span class="keyword">import</span> csrf_exempt</span><br><span class="line"><span class="keyword">from</span> django.conf <span class="keyword">import</span> settings</span><br><span class="line"><span class="keyword">from</span> ipaddress <span class="keyword">import</span> ip_address, ip_network</span><br><span class="line"><span class="keyword">import</span> hmac</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_client_ip</span><span class="params">(request)</span>:</span></span><br><span class="line">    x_forwarded_for = request.META.get(<span class="string">'HTTP_X_FORWARDED_FOR'</span>)</span><br><span class="line">    <span class="keyword">if</span> x_forwarded_for:</span><br><span class="line">        ip = x_forwarded_for.split(<span class="string">','</span>)[<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        ip = request.META.get(<span class="string">'REMOTE_ADDR'</span>)</span><br><span class="line">    <span class="keyword">return</span> ip</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@csrf_exempt</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">webhook</span><span class="params">(request)</span>:</span></span><br><span class="line">    client_ip = get_client_ip(request)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> network <span class="keyword">in</span> settings.IP_WHITELIST:</span><br><span class="line">        <span class="keyword">if</span> ip_address(client_ip) <span class="keyword">in</span> ip_network(network):</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> HttpResponseForbidden(<span class="string">'你没有权限访问！'</span>)</span><br><span class="line"></span><br><span class="line">    header_signature = request.META.get(<span class="string">'HTTP_X_HUB_SIGNATURE'</span>)</span><br><span class="line">    <span class="keyword">if</span> header_signature <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="keyword">return</span> HttpResponseForbidden(<span class="string">'你没有权限访问！'</span>)</span><br><span class="line"></span><br><span class="line">    hash_type, signature = header_signature.split(<span class="string">'='</span>)</span><br><span class="line">    <span class="keyword">if</span> hash_type != <span class="string">'sha1'</span>:</span><br><span class="line">        <span class="keyword">return</span> HttpResponseForbidden(<span class="string">'不支持的加密方式'</span>, status=<span class="number">501</span>)</span><br><span class="line"></span><br><span class="line">    mac = hmac.new(settings.GITHUB_WEBHOOK_KEY.encode(<span class="string">'utf-8'</span>), request.body, hash_type).hexdigest()</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> hmac.compare_digest(mac, signature):</span><br><span class="line">        <span class="keyword">return</span> HttpResponseForbidden(<span class="string">'你没有权限访问！'</span>)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> HttpResponse(<span class="string">'pong'</span>)</span><br></pre></td></tr></table></figure>
<p>注意上面 <code>hmac.compare_digest(a, b)</code> 的结果和 <code>a == b</code> 一模一样，用于校验字符串。那为什么还要用这个函数呢？网上几乎查询不到相关资料，Python 官方文档写的也太简单晦涩了，直到看到 <a href="https://news.ycombinator.com/item?id=11119154" target="_blank" rel="noopener">https://news.ycombinator.com/item?id=11119154</a> 这个帖子才知道了原因。因为 a == b 在判断字符串是否相等时，如果第一个字符不相等，就立即返回 <code>False</code>，正常的算法都是要求越快越好，这也是理所当然的，但是 <code>hmac.compare_digest(a, b)</code> 会从头到尾判断，这样反而降低了运算速度。那为什么要这么做呢？</p>
<p>原来破解有一种思想叫做时序攻击 <code>timing attack</code>，如果用 <code>a == b</code>，会大大减少破解时间，相当于降低破解难度，而 <code>hmac.compare_digest(a, b)</code> 在一定程度上防御了时序攻击。</p>
<p>当然 Django 也内置了类似功能：<code>django.utils.crypto.constant_time_compare</code>，内部就是调用的 <code>hmac.compare_digest</code></p>
<p>可以看看知乎上的讨论：如何通俗地解释时序攻击</p>
<h2 id="只允许-POST-请求"><a href="#只允许-POST-请求" class="headerlink" title="只允许 POST 请求"></a>只允许 <code>POST</code> 请求</h2><p>这又是一种限制</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> hmac</span><br><span class="line"><span class="keyword">from</span> ipaddress <span class="keyword">import</span> ip_address, ip_network</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> django.conf <span class="keyword">import</span> settings</span><br><span class="line"><span class="keyword">from</span> django.http <span class="keyword">import</span> HttpResponse, HttpResponseForbidden</span><br><span class="line"><span class="keyword">from</span> django.views.decorators.csrf <span class="keyword">import</span> csrf_exempt</span><br><span class="line"><span class="keyword">from</span> django.views.decorators.http <span class="keyword">import</span> require_POST</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="meta">@require_POST</span></span><br><span class="line"><span class="meta">@csrf_exempt</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">webhook</span><span class="params">(request)</span>:</span></span><br><span class="line">    ...</span><br><span class="line">    <span class="keyword">return</span> HttpResponse(<span class="string">'pong'</span>)</span><br></pre></td></tr></table></figure>

<h1 id="处理-Github-事件"><a href="#处理-Github-事件" class="headerlink" title="处理 Github 事件"></a>处理 Github 事件</h1><p>上面已经提到过这个请求头 <code>X-GitHub-Event</code>，有哪些事件类型，建议参考 <a href="https://developer.github.com/v3/activity/events/types/#event-types--payloads" target="_blank" rel="noopener">Event Types &amp; Payloads</a>。</p>
<p>在这里考虑到只部署博客，我们只接受 push 事件。然后利用子进程进行 <code>git pull</code> 操作。</p>
<p>先在服务器的 <code>Home</code> 目录下克隆博客的静态页面，</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> git <span class="built_in">clone</span> https://github.com/yszar/yszar.github.io blog</span></span><br></pre></td></tr></table></figure>
<p>然后配置好 <code>caddy</code> 或 <code>nginx</code>，这里不讲怎么配置 Web 服务器。</p>
<p>最终代码如下：</p>
<figure class="highlight py"><table><tr><td class="code"><pre><span class="line"><span class="keyword">import</span> hmac</span><br><span class="line"><span class="keyword">from</span> ipaddress <span class="keyword">import</span> ip_address, ip_network</span><br><span class="line"></span><br><span class="line"><span class="keyword">from</span> django.conf <span class="keyword">import</span> settings</span><br><span class="line"><span class="keyword">from</span> django.http <span class="keyword">import</span> HttpResponse, HttpResponseForbidden</span><br><span class="line"><span class="keyword">from</span> django.views.decorators.csrf <span class="keyword">import</span> csrf_exempt</span><br><span class="line"><span class="keyword">from</span> django.views.decorators.http <span class="keyword">import</span> require_POST</span><br><span class="line"><span class="keyword">import</span> subprocess</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">get_client_ip</span><span class="params">(request)</span>:</span></span><br><span class="line">    x_forwarded_for = request.META.get(<span class="string">'HTTP_X_FORWARDED_FOR'</span>)</span><br><span class="line">    <span class="keyword">if</span> x_forwarded_for:</span><br><span class="line">        ip = x_forwarded_for.split(<span class="string">','</span>)[<span class="number">0</span>]</span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        ip = request.META.get(<span class="string">'REMOTE_ADDR'</span>)</span><br><span class="line">    <span class="keyword">return</span> ip</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">sync_blog</span><span class="params">()</span>:</span></span><br><span class="line">    cwd = <span class="string">'/home/ubuntu/blog'</span></span><br><span class="line">    subprocess.run(<span class="string">'git pull'</span>.split(), cwd=cwd)</span><br><span class="line">    subprocess.run(<span class="string">'git reset --hard origin/master'</span>.split(), cwd=cwd)</span><br><span class="line"></span><br><span class="line"></span><br><span class="line"><span class="meta">@require_POST</span></span><br><span class="line"><span class="meta">@csrf_exempt</span></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">webhook</span><span class="params">(request)</span>:</span></span><br><span class="line">    client_ip = get_client_ip(request)</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> network <span class="keyword">in</span> settings.IP_WHITELIST:</span><br><span class="line">        <span class="keyword">if</span> ip_address(client_ip) <span class="keyword">in</span> ip_network(network):</span><br><span class="line">            <span class="keyword">break</span></span><br><span class="line">    <span class="keyword">else</span>:</span><br><span class="line">        <span class="keyword">return</span> HttpResponseForbidden(<span class="string">'你没有权限访问！'</span>)</span><br><span class="line"></span><br><span class="line">    header_signature = request.META.get(<span class="string">'HTTP_X_HUB_SIGNATURE'</span>)</span><br><span class="line">    <span class="keyword">if</span> header_signature <span class="keyword">is</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="keyword">return</span> HttpResponseForbidden(<span class="string">'你没有权限访问！'</span>)</span><br><span class="line"></span><br><span class="line">    hash_type, signature = header_signature.split(<span class="string">'='</span>)</span><br><span class="line">    <span class="keyword">if</span> hash_type != <span class="string">'sha1'</span>:</span><br><span class="line">        <span class="keyword">return</span> HttpResponseForbidden(<span class="string">'不支持的加密方式'</span>, status=<span class="number">501</span>)</span><br><span class="line"></span><br><span class="line">    mac = hmac.new(settings.GITHUB_WEBHOOK_KEY.encode(<span class="string">'utf-8'</span>), request.body, hash_type).hexdigest()</span><br><span class="line">    <span class="keyword">if</span> <span class="keyword">not</span> hmac.compare_digest(mac, signature):</span><br><span class="line">        <span class="keyword">return</span> HttpResponseForbidden(<span class="string">'你没有权限访问！'</span>)</span><br><span class="line"></span><br><span class="line">    event = request.META.get(<span class="string">'HTTP_X_GITHUB_EVENT'</span>, <span class="string">'ping'</span>)</span><br><span class="line">    <span class="keyword">if</span> event == <span class="string">'ping'</span>:</span><br><span class="line">        <span class="keyword">return</span> HttpResponse(<span class="string">'pong'</span>)</span><br><span class="line">    <span class="keyword">if</span> event == <span class="string">'push'</span>:</span><br><span class="line">        sync_blog()</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> HttpResponse(status=<span class="number">204</span>)</span><br></pre></td></tr></table></figure>
<p>以后每次在本地推送博客到 Github 后，VPS 的内容也跟着更新。</p>
<figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> hexo d</span></span><br></pre></td></tr></table></figure>
]]></content>
      <categories>
        <category>Python</category>
      </categories>
      <tags>
        <tag>python</tag>
        <tag>github</tag>
        <tag>django</tag>
      </tags>
  </entry>
</search>
